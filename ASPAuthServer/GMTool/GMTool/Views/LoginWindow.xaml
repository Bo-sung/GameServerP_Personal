<Window x:Class="GMTool.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        xmlns:controls="clr-namespace:GMTool.Views.Controls"
        Title="GM Tool - 로그인"
        Width="700"
        Height="600"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResize"
        ui:WindowHelper.UseModernWindowStyle="True">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />      <!-- 로그인 영역 -->
            <RowDefinition Height="200" />    <!-- 로그 영역 (고정) -->
        </Grid.RowDefinitions>

        <!-- 로그인 영역 -->
        <Border Grid.Row="0" Background="{DynamicResource SystemControlPageBackgroundAltHighBrush}">
            <StackPanel VerticalAlignment="Center"
                        HorizontalAlignment="Center"
                        Width="350">

                <TextBlock Text="🎮 GM Tool"
                           FontSize="32"
                           FontWeight="SemiBold"
                           HorizontalAlignment="Center"
                           Margin="0,0,0,48" />

                <!-- Username -->
                <TextBlock Text="사용자명"
                           Margin="0,0,0,8"
                           FontSize="14" />
                <TextBox x:Name="UsernameTextBox"
                         ui:ControlHelper.PlaceholderText="관리자 사용자명을 입력하세요"
                         Height="36"
                         FontSize="14"
                         Margin="0,0,0,20" />

                <!-- Password -->
                <TextBlock Text="비밀번호"
                           Margin="0,0,0,8"
                           FontSize="14" />
                <PasswordBox x:Name="PasswordBox"
                             ui:ControlHelper.PlaceholderText="비밀번호를 입력하세요"
                             Height="36"
                             FontSize="14"
                             Margin="0,0,0,8" />

                <!-- Remember Device (선택사항) -->
                <CheckBox Content="이 장치 기억하기"
                          Margin="0,8,0,20" />

                <!-- 에러 메시지 영역 -->
                <Border x:Name="ErrorMessageBorder"
                        Background="#FFEBEE"
                        BorderBrush="#F44336"
                        BorderThickness="1"
                        CornerRadius="4"
                        Padding="12"
                        Margin="0,0,0,20"
                        Visibility="Collapsed">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="❌"
                                   FontSize="16"
                                   VerticalAlignment="Center"
                                   Margin="0,0,8,0" />
                        <TextBlock x:Name="ErrorMessageText"
                                   TextWrapping="Wrap"
                                   Foreground="#C62828"
                                   VerticalAlignment="Center" />
                    </StackPanel>
                </Border>

                <!-- 로그인 버튼 -->
                <Button x:Name="LoginButton"
                        Content="로그인"
                        Style="{StaticResource AccentButtonStyle}"
                        Height="40"
                        FontSize="15"
                        FontWeight="SemiBold"
                        Margin="0,0,0,16" />

                <!-- 로딩 인디케이터 -->
                <ui:ProgressRing x:Name="LoadingRing"
                                 IsActive="False"
                                 Width="36"
                                 Height="36"
                                 Visibility="Collapsed"
                                 HorizontalAlignment="Center" />

                <!-- 추가 정보 -->
                <TextBlock Text="관리자 계정으로만 로그인할 수 있습니다."
                           FontSize="12"
                           Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}"
                           HorizontalAlignment="Center"
                           Margin="0,16,0,0" />
            </StackPanel>
        </Border>

        <!-- 로그 뷰어 (하단 고정) -->
        <controls:LogViewer Grid.Row="1" />
    </Grid>
</Window>
