<Page x:Class="GMTool.Views.Pages.UserListPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.modernwpf.com/2019"
      xmlns:vm="clr-namespace:GMTool.ViewModels"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      d:DataContext="{d:DesignInstance Type=vm:UserListViewModel}"
      Title="사용자 관리">

    <Grid Margin="32">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />   <!-- 헤더 -->
            <RowDefinition Height="Auto" />   <!-- 검색 바 -->
            <RowDefinition Height="*" />      <!-- 사용자 목록 -->
            <RowDefinition Height="Auto" />   <!-- 페이지네이션 -->
        </Grid.RowDefinitions>

        <!-- 헤더 -->
        <StackPanel Grid.Row="0" Margin="0,0,0,24">
            <TextBlock Text="👥 사용자 관리"
                       FontSize="28"
                       FontWeight="SemiBold"
                       Margin="0,0,0,8" />
            <TextBlock Text="전체 사용자 목록을 조회하고 관리하세요"
                       FontSize="14"
                       Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}" />
        </StackPanel>

        <!-- 검색 바 -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <!-- 검색 -->
            <TextBox Grid.Column="0"
                     Text="{Binding SearchQuery, UpdateSourceTrigger=PropertyChanged}"
                     ui:ControlHelper.PlaceholderText="사용자명 또는 이메일로 검색..."
                     Height="40"
                     VerticalContentAlignment="Center"
                     FontSize="14"
                     Margin="0,0,12,0" />

            <!-- 필터 -->
            <ComboBox Grid.Column="1"
                      SelectedIndex="{Binding StatusFilter}"
                      Height="40"
                      Width="150"
                      VerticalContentAlignment="Center"
                      Margin="0,0,12,0">
                <ComboBoxItem Content="전체 상태" />
                <ComboBoxItem Content="✅ 활성" />
                <ComboBoxItem Content="❌ 비활성" />
                <ComboBoxItem Content="🔒 잠김" />
            </ComboBox>

            <!-- 검색 버튼 -->
            <Button Grid.Column="2"
                    Command="{Binding SearchCommand}"
                    Content="🔍 검색"
                    Style="{StaticResource AccentButtonStyle}"
                    Height="40"
                    Padding="24,0"
                    IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBooleanConverter}}" />
        </Grid>

        <!-- 사용자 목록 -->
        <Border Grid.Row="2"
                Background="White"
                BorderBrush="#E0E0E0"
                BorderThickness="1"
                CornerRadius="8">
            <DataGrid ItemsSource="{Binding Users}"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      CanUserResizeRows="False"
                      SelectionMode="Single"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      HorizontalGridLinesBrush="#F0F0F0"
                      BorderThickness="0"
                      VirtualizingPanel.IsVirtualizing="True"
                      VirtualizingPanel.VirtualizationMode="Recycling"
                      EnableRowVirtualization="True">

                <DataGrid.Columns>
                    <!-- ID -->
                    <DataGridTextColumn Header="ID"
                                        Binding="{Binding Id}"
                                        Width="80" />

                    <!-- 사용자명 -->
                    <DataGridTextColumn Header="사용자명"
                                        Binding="{Binding Username}"
                                        Width="150" />

                    <!-- 이메일 -->
                    <DataGridTextColumn Header="이메일"
                                        Binding="{Binding Email}"
                                        Width="*" />

                    <!-- 상태 -->
                    <DataGridTextColumn Header="상태"
                                        Binding="{Binding IsActive}"
                                        Width="100" />

                    <!-- 생성일 -->
                    <DataGridTextColumn Header="가입일"
                                        Binding="{Binding CreatedAt}"
                                        Width="140" />

                    <!-- 마지막 로그인 -->
                    <DataGridTextColumn Header="마지막 로그인"
                                        Binding="{Binding LastLoginAt}"
                                        Width="140" />

                    <!-- 액션 -->
                    <DataGridTemplateColumn Header="액션" Width="100">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="상세"
                                        Height="28"
                                        Padding="12,0"
                                        FontSize="12" />
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>

                <!-- 데이터가 없을 때 -->
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Height" Value="48" />
                    </Style>
                </DataGrid.RowStyle>
            </DataGrid>
        </Border>

        <!-- 페이지네이션 -->
        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    Margin="0,24,0,0">
            <Button Command="{Binding PreviousPageCommand}"
                    Content="◀ 이전"
                    Width="80"
                    Height="36"
                    Margin="0,0,8,0" />

            <Border Background="White"
                    BorderBrush="#E0E0E0"
                    BorderThickness="1"
                    CornerRadius="4"
                    Padding="16,8"
                    Margin="0,0,8,0">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{Binding CurrentPage}"
                               FontSize="14"
                               FontWeight="SemiBold"
                               VerticalAlignment="Center" />
                    <TextBlock Text="/"
                               FontSize="14"
                               Foreground="#999"
                               Margin="4,0"
                               VerticalAlignment="Center" />
                    <TextBlock Text="{Binding TotalPages}"
                               FontSize="14"
                               Foreground="#666"
                               VerticalAlignment="Center" />
                </StackPanel>
            </Border>

            <Button Command="{Binding NextPageCommand}"
                    Content="다음 ▶"
                    Width="80"
                    Height="36" />

            <Border Background="#F5F5F5"
                    BorderBrush="#E0E0E0"
                    BorderThickness="1"
                    CornerRadius="4"
                    Padding="12,8"
                    Margin="16,0,0,0">
                <TextBlock Text="{Binding TotalCount, StringFormat='총 {0}명'}"
                           FontSize="14"
                           Foreground="#666"
                           VerticalAlignment="Center" />
            </Border>
        </StackPanel>
    </Grid>
</Page>
