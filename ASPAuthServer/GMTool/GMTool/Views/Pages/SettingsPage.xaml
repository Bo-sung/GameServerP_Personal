<Page x:Class="GMTool.Views.Pages.SettingsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.modernwpf.com/2019"
      Title="설정">

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="32" MaxWidth="800">

            <!-- 헤더 -->
            <StackPanel Margin="0,0,0,32">
                <TextBlock Text="⚙️ 설정"
                           FontSize="28"
                           FontWeight="SemiBold"
                           Margin="0,0,0,8" />
                <TextBlock Text="GM Tool 설정을 관리하세요"
                           FontSize="14"
                           Foreground="{DynamicResource SystemControlForegroundBaseMediumBrush}" />
            </StackPanel>

            <!-- API 서버 설정 -->
            <Border Background="White"
                    BorderBrush="#E0E0E0"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="24"
                    Margin="0,0,0,16">
                <StackPanel>
                    <TextBlock Text="🌐 API 서버 설정"
                               FontSize="18"
                               FontWeight="SemiBold"
                               Margin="0,0,0,16" />

                    <!-- API Base URL -->
                    <TextBlock Text="API Base URL"
                               FontSize="14"
                               Margin="0,0,0,8" />
                    <TextBox x:Name="ApiBaseUrlTextBox"
                             Text="http://localhost:5000"
                             Height="40"
                             VerticalContentAlignment="Center"
                             FontSize="14"
                             Margin="0,0,0,8" />
                    <TextBlock Text="AuthServer의 기본 URL을 입력하세요"
                               FontSize="12"
                               Foreground="#999"
                               Margin="0,0,0,16" />

                    <!-- 테스트 버튼 -->
                    <Button x:Name="TestConnectionButton"
                            Content="🔌 연결 테스트"
                            Width="150"
                            Height="36"
                            HorizontalAlignment="Left" />
                </StackPanel>
            </Border>

            <!-- 로그 설정 -->
            <Border Background="White"
                    BorderBrush="#E0E0E0"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="24"
                    Margin="0,0,0,16">
                <StackPanel>
                    <TextBlock Text="📝 로그 설정"
                               FontSize="18"
                               FontWeight="SemiBold"
                               Margin="0,0,0,16" />

                    <!-- 최대 로그 개수 -->
                    <TextBlock Text="최대 로그 개수"
                               FontSize="14"
                               Margin="0,0,0,8" />
                    <StackPanel Orientation="Horizontal">
                        <Slider x:Name="MaxLogCountSlider"
                                Minimum="100"
                                Maximum="1000"
                                Value="500"
                                Width="300"
                                VerticalAlignment="Center"
                                Margin="0,0,16,0" />
                        <TextBlock x:Name="MaxLogCountText"
                                   Text="500개"
                                   FontSize="14"
                                   VerticalAlignment="Center" />
                    </StackPanel>
                    <TextBlock Text="로그 뷰어에 표시할 최대 로그 개수 (성능 최적화)"
                               FontSize="12"
                               Foreground="#999"
                               Margin="0,8,0,16" />

                    <!-- 자동 스크롤 -->
                    <CheckBox Content="새 로그 발생 시 자동 스크롤"
                              IsChecked="True"
                              Margin="0,0,0,8" />

                    <!-- Debug 로그 표시 -->
                    <CheckBox Content="Debug 레벨 로그 표시"
                              IsChecked="True" />
                </StackPanel>
            </Border>

            <!-- 애플리케이션 정보 -->
            <Border Background="White"
                    BorderBrush="#E0E0E0"
                    BorderThickness="1"
                    CornerRadius="8"
                    Padding="24">
                <StackPanel>
                    <TextBlock Text="ℹ️ 애플리케이션 정보"
                               FontSize="18"
                               FontWeight="SemiBold"
                               Margin="0,0,0,16" />

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="150" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0"
                                   Text="버전:"
                                   FontSize="14"
                                   Foreground="#666"
                                   Margin="0,0,0,8" />
                        <TextBlock Grid.Row="0" Grid.Column="1"
                                   Text="1.0.0"
                                   FontSize="14"
                                   Margin="0,0,0,8" />

                        <TextBlock Grid.Row="1" Grid.Column="0"
                                   Text="프레임워크:"
                                   FontSize="14"
                                   Foreground="#666"
                                   Margin="0,0,0,8" />
                        <TextBlock Grid.Row="1" Grid.Column="1"
                                   Text=".NET 8.0 WPF"
                                   FontSize="14"
                                   Margin="0,0,0,8" />

                        <TextBlock Grid.Row="2" Grid.Column="0"
                                   Text="UI 라이브러리:"
                                   FontSize="14"
                                   Foreground="#666" />
                        <TextBlock Grid.Row="2" Grid.Column="1"
                                   Text="ModernWpfUI"
                                   FontSize="14" />
                    </Grid>
                </StackPanel>
            </Border>

            <!-- 저장 버튼 -->
            <Button x:Name="SaveSettingsButton"
                    Content="💾 설정 저장"
                    Style="{StaticResource AccentButtonStyle}"
                    Height="44"
                    FontSize="15"
                    Margin="0,24,0,0" />
        </StackPanel>
    </ScrollViewer>
</Page>
